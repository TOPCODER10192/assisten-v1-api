/timesheets/{timesheetId}:
  get:
    tags:
      - Address
    summary: Show Address
    parameters:
      - name: timesheetId
        description: Address ID
        in: path
        required: false
        type: string
    responses:
      200:
        description: Response
        content:
          status: true
          user: {}
